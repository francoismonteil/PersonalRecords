<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.personalrecords">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_appli"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_appli_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FillActivity"></activity>
        <activity android:name=".IndicatorsAddActivity" />
        <activity android:name=".InformationsActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SpaceAddActivity" />
        <activity android:name=".IndicatorsActivity" />
        <activity android:name=".SpacesActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>